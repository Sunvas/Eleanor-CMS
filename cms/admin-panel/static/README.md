### English
Template files of unit `static` (static pages) for the admin panel. Each file contains HTML markup of page with variables.

### Русский язык
Шаблоны панели администратора для юнита `static` (статические страницы). Каждый файл содержит HTML разметку страницы с переменными. 