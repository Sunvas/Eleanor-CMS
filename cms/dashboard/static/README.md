### English
Template files of unit `static` (static pages) for the dashboard. Each file contains HTML markup of page with variables.

### Русский язык
Шаблоны дашборда для юнита `static` (статические страницы). Каждый файл содержит HTML разметку страницы с переменными. 