### English
Template files of unit `users` for the dashboard. Each file contains HTML markup of page with variables.

### Русский язык
Шаблоны дашборда для юнита `users` (пользователи). Каждый файл содержит HTML разметку страницы с переменными. 